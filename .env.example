# Environment Variables Example for MediaDrop (media-to-mp3)
# No required secrets for core public oEmbed and client-side processing!
# Optional YouTube Data API v3 key (only if extended quota metadata is ever required):
# YOUTUBE_API_KEY=your_optional_api_key_here
NEXT_PUBLIC_APP_NAME=MediaDrop
